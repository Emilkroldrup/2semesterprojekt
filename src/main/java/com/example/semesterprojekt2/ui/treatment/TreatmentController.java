package com.example.semesterprojekt2.ui.treatment;

import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;


public class TreatmentController {

    @FXML
    Label treatmentsLabel = new Label (); // title
    @FXML
    Button trimButton = new Button(); // opens booking scene
    @FXML
    Button hairdyeButton = new Button(); // opens booking scene
    @FXML
    Button hairwashButton = new Button(); // opens booking scene
    @FXML
    Button shaveButton = new Button(); // opens booking scene
    @FXML
    Button requestButton = new Button(); // opens a text editor for the customer to request a custom treatment
    @FXML
    Button returnButton = new Button(); // returns to former scene


}
